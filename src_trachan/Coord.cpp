#include "Coord.h"
using namespace std;

/*
template <typename T> int Coord<T>::dim = 3;

template <typename T> Coord<T>::Coord(){
  x.reserve(dim);
  x.push_back(0.0);
  x.push_back(0.0);
  x.push_back(0.0);
}
template <typename T> Coord<T>::Coord(const T in_x, const T in_y, const T in_z){
  x.reserve(dim);
  x.push_back(in_x);
  x.push_back(in_y);
  x.push_back(in_z);
}
*/

/*
int Coord::dim = 3;

Coord::Coord(){
  x.reserve(dim);
  x.push_back(0.0);
  x.push_back(0.0);
  x.push_back(0.0);
}
Coord::Coord(const float in_x, const float in_y, const float in_z){
  x.reserve(dim);
  x.push_back(in_x);
  x.push_back(in_y);
  x.push_back(in_z);
}
*/
